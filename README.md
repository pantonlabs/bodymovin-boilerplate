# Follow these steps to load a new animation for testing using bodymovin

- Render the video on AE
- Delete the JSON file & images folder
- Copy & paste the rendered animation in the project directory
- Start local server 
